type User {
    id: ID!
    name: String!
    email: String!
    type: Int!
}

type Query {
    user: [User]!
}

input NewUser {
    name: String!
    email: String!
    password: String!
}

type Mutation {
    createUser(input: NewUser!): User!
}